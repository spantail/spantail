packages:
  - apps/*
  - packages/*

# pnpm 10 blocks dependency build scripts by default; allow only what we need.
allowBuilds:
  workerd: true
  esbuild: true
  "@biomejs/biome": true
  "@tailwindcss/oxide": true
  sharp: true
